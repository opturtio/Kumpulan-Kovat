[tool.poetry]
name = "bibtex-generator"
version = "0.1.0"
description = "Web app for generating bibtex-files"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "bibtex_generator"}]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^2.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
